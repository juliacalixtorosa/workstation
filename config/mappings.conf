# ==========================================================
# Workstation Configuration Mappings
#
# Format:
# <repository_path>|<target_path>
#
# Repository paths are relative to the repository root.
# Target paths may use "~" for the user's home directory.
# ==========================================================

config/bash/.bashrc|~/.bashrc
config/git/.gitconfig|~/.gitconfig
config/starship/starship.toml|~/.config/starship.toml